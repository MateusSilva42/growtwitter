import { Tweet } from "../classes/Tweet";
import { User } from "../classes/User";

// Dados dos usuários e tweets
export const Users: Array<User> = [

];

export const Tweets: Array<Tweet> = [

];

// Exportar os módulos
module.exports = { Users, Tweets };
